<% content_for :sidebar do %>
  <%= render 'cadastro/sidebar' %>
<% end %>

<% provide(:url, cadastro_clientes_path) %>
<% provide(:new_url, new_cadastro_cliente_path) %>
<% provide(:search_field, :nome_or_razao_social_cont) %>
<% provide(:placeholder, 'Nome ou Razão Social') %>

<div class="container-fluid">

  <!-- header -->
  <div class="row">
    <div class="col-lg-12">
      <div class="page-header">
        <h1>Clientes</h1>
      </div>
    </div>
  </div>

  <!-- search form -->
  <div class="row">
    <div class="col-lg-12">
      <%= render 'shared/search_form' %>
    </div>
  </div>

  <!-- table view -->
  <div class="row">
    <div class="col-lg-12">
      <div id="cli-tableview"><%= render 'table' %></div>
    </div>
  </div>

</div>
