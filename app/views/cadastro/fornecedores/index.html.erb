<% provide(:url, cadastro_fornecedores_path) %>
<% provide(:search_field, :nome_or_razao_social_cont) %>
<% provide(:placeholder, 'Nome ou Razão Social') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <%= fa_tag 'fa fa-align-justify' %>
        Cadastro<strong>Fornecedores</strong>
      </div>

      <div class="card-block">
        <div class="row">
          <div class="col-sm-4">
            <%= render 'shared/search_form' %>
          </div>
          <div class="col-sm-8 text-right">
            <%= link_to 'Novo Fornecedor',
                        new_cadastro_fornecedor_path,
                        remote: true,
                        class: 'btn btn-success' %>
          </div>
        </div>

        <div id="frn-tableview">
          <%= render 'table' %>
        </div>
      </div>
    </div>
  </div>
</div>

