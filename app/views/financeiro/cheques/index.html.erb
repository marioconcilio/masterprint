<% content_for :sidebar do %>
  <%= render 'financeiro/sidebar' %>
<% end %>

<% provide(:url, financeiro_cheques_path) %>
<% provide(:search_field, :numero_text_or_emitente_cont) %>
<% provide(:placeholder, 'Número ou emitente') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        Financeiro<strong>Cheques</strong>
      </div>

      <div class="card-block">
        <div class="row">
          <div class="col-sm-4">
            <%= render 'shared/search_form' %>
          </div>
          <div class="col-sm-8 text-right">
            <%= link_to 'Cadastrar Cheque',
                        new_financeiro_cheque_path,
                        remote: true,
                        class: 'btn btn-success' %>
          </div>
        </div>

        <div id="chq-tableview">
          <%= render 'table' %>
        </div>
      </div>
    </div>
  </div>
</div>
