<% content_for :navbar do %>
  <%= render 'financeiro/navbar' %>
<% end %>

<% provide(:url, financeiro_cheques_path) %>
<% provide(:new_url, new_financeiro_cheque_path) %>
<% provide(:search_field, :numero_or_emitente_cont) %>
<% provide(:placeholder, 'Número ou emitente') %>

<div class="container-fluid">

  <!-- header -->
  <div class="row">
    <div class="col-lg-12">
      <div class="page-header">
        <h1>Cheques</h1>
      </div>
    </div>
  </div>

  <!-- search form -->
  <div class="row">
    <div class="col-lg-12">
      <%= render 'shared/search_form' %>
    </div>
  </div>

  <!-- table view -->
  <div class="row row-margin">
    <div class="col-lg-12">
      <div id="chq-tableview"><%= render 'table' %></div>
    </div>
  </div>

</div>
