<% content_for :navbar do %>
  <%= render 'financeiro/navbar' %>
<% end %>

<% provide(:url, financeiro_depositos_path) %>
<% provide(:new_url, new_financeiro_deposito_path) %>
<% provide(:search_field, :titular_cont) %>
<% provide(:placeholder, 'Titular') %>

<div class="container-fluid">

  <!-- header -->
  <div class="row">
    <div class="col-lg-12">
      <div class="page-header">
        <h1>Depósitos</h1>
      </div>
    </div>
  </div>

  <!-- search form -->
  <div class="row">
    <div class="col-lg-12">
      <%= render 'shared/search_form' %>
    </div>
  </div>

  <!-- table view -->
  <div class="row row-margin">
    <div class="col-lg-12">
      <div id="dpo-tableview"><%= render 'table' %></div>
    </div>
  </div>

</div>
