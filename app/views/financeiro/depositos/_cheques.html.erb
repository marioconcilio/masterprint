<table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Banco</th>
      <th>Número</th>
      <th>Emitente</th>
      <th class="text-right">Valor</th>
    </tr>
  </thead>
  <tbody>
    <% @cheques.try(:each) do |c| %>
      <tr>
        <td><%= format_banco(c['banco']) %></td>
        <td><%= format_numero(c['numero']) %></td>
        <td><%= c['emitente'] %></td>
        <td class="text-right"><%= format_currency(c['valor']) %></td>
        <td><%= link_to '',
                        ch_remove_financeiro_depositos_path(c['id']),
                        method: :delete,
                        remote: true,
                        class: 'fa fa-trash no-underline' %></td>
      </tr>
    <% end %>
  </tbody>
</table>



