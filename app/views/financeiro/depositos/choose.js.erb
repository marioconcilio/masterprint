$('#ch_search').blur();

var modal = $('#modal-window-lg');
modal.attr('class', 'modal fade');
modal.find('.modal-content').html('<%= j(render "choose") %>');
modal.modal();

$('.form-cheque').on('submit', function() {
  modal.modal('hide');
});
