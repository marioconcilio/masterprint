<!-- pagination -->
<div class="row">
  <div class="col-lg-6">
    <%= paginate @calendarios, remote: true %>
  </div>
</div>
<!-- /.row -->

<div class="row">
  <div class="col-lg-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th class="w-10">
            <%= sort_link @search, :pagina, 'Página', {}, { remote: true, method: :get } %>
          </th>
          <th class="w-10">
            <%= sort_link @search, :tipo, 'Tipo', {}, { remote: true, method: :get } %>
          </th>
          <th class="w-20">
            <%= sort_link @search, :referencia, 'Referência', {}, { remote: true, method: :get } %>
          </th>
          <th class="w-40">
            <%= sort_link @search, :descricao, 'Descrição', {}, { remote: true, method: :get } %>
          </th>
          <th class="w-10 text-right">
            <%= sort_link @search, :preco, 'Preço', {}, { remote: true, method: :get } %>
          </th>
          <th class="w-10 text-center">
            <%= sort_link @search, :un, 'Unidade', {}, { remote: true, method: :get } %>
          </th>
          <% if logged_in? %>
            <th class="w-5"><br></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% @calendarios.each do |c| %>
          <tr>
            <td> <%= c.pagina %> </td>
            <td> <%= c.tipo %> </td>
            <td> <%= c.referencia %> </td>
            <td> <%= raw c.descricao %> </td>
            <td class="text-right"> <%= format_currency c.preco %> </td>
            <td class="text-center"> <%= c.un %> </td>
            <% if logged_in? %>
              <td class="text-right">
                <%= link_details_to '', edit_tiktak_calendario_path(c) %>
              </td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<!-- pagination -->
<div class="row">
  <div class="col-lg-6">
    <%= paginate @calendarios, remote: true %>
  </div>
</div>
<!-- /.row -->
