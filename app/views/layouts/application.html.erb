<!DOCTYPE html>
<html>
  <head>
    <title>Masterprint</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
        <!-- We use the fluid option here to avoid overriding the fixed width of a normal container within the narrow content columns. -->
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-6">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Masterprint</a>
          </div>

          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-6">
            <ul class="nav navbar-nav">
              <li class="<%= active_class(estoque_blanquetas_path) %>">
                <%= link_to "Blanquetas", estoque_blanquetas_path %>
              </li>
              <li class="<%= active_class(estoque_chapas_path) %>">
                <%= link_to "Chapas", estoque_chapas_path %>
              </li>
              <li class="<%= active_class(estoque_envelopes_path) %>">
                <%= link_to "Envelopes", estoque_envelopes_path %>
              </li>
              <li class="<%= active_class(estoque_quimicos_path) %>">
                <%= link_to "Químicos", estoque_quimicos_path %>
              </li>
              <li class="<%= active_class(estoque_tintas_path) %>">
                <%= link_to "Tintas", estoque_tintas_path %>
              </li>
              <li class="<%= active_class(estoque_variados_path) %>">
                <%= link_to "Variados", estoque_variados_path %>
              </li>
            </ul>
          </div><!-- /.navbar-collapse -->
        </div>
      </nav>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
