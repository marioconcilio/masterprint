<% content_for :navbar do %>
  <%= render 'estoque/navbar' %>
<% end %>

<% provide(:url, estoque_papeis_path) %>
<% provide(:new_url, new_estoque_papel_path) %>
<% provide(:search_field, :papel_tipo_tipo_or_grs_cont) %>
<% provide(:placeholder, 'Tipo ou gramatura') %>

<div class="container-fluid">

  <!-- header -->
  <div class="row">
    <div class="col-lg-12">
      <div class="page-header">
        <h1>Papéis</h1>
      </div>
    </div>
  </div>

  <!-- search form -->
  <div class="row">
    <div class="col-lg-12">
      <%= render 'shared/search_form' %>
    </div>
  </div>

  <!-- table view -->
  <div class="row">
    <div class="col-lg-12">
      <div id="ppl-tableview"><%= render 'table' %></div>
    </div>
  </div>

</div>
