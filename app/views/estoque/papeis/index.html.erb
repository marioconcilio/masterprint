<% content_for :sidebar do %>
  <%= render 'estoque/sidebar' %>
<% end %>

<% provide(:url, estoque_papeis_path) %>
<% provide(:search_field, :papel_tipo_tipo_or_grs_cont) %>
<% provide(:placeholder, 'Tipo ou gramatura') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <%= fa_tag 'fa fa-align-justify' %>
        Estoque<strong>Papéis</strong>
      </div>

      <div class="card-block">
        <div class="row">
          <div class="col-sm-4">
            <%= render 'shared/search_form' %>
          </div>

          <% if logged_in? %>
            <div class="col-sm-8 text-right">
              <%= link_to 'Novo Papel',
                          new_estoque_papel_path,
                          remote: true,
                          class: 'btn btn-success' %>
            </div>
          <% end %>
        </div>

        <div id="ppl-tableview">
          <%= render 'table' %>
        </div>
      </div>
    </div>
  </div>
</div>
