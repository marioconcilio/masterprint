<% content_for :sidebar do %>
  <%= render 'estoque/sidebar' %>
<% end %>

<% provide(:url, estoque_blanquetas_path) %>
<% provide(:new_url, new_estoque_blanqueta_path) %>
<% provide(:search_field, :nome_or_larg_or_comp_cont) %>
<% provide(:placeholder, 'Nome ou tamanho') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">Blanquetas</div>
      <div class="card-block" id="blq-tableview">
        <div class="row">
          <div class="col-sm-6">
          </div>
          <div class="col-sm-6">
            <%= render 'shared/search_form' %>
          </div>
        </div>

        <%= render 'table' %>
      </div>
    </div>
  </div>
</div>
