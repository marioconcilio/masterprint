<% provide(:url, estoque_tintas_path) %>
<% provide(:search_field, @search_field) %>
<% provide(:placeholder, 'Nome ou marca') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <%= fa_tag 'fa fa-align-justify' %>
        Estoque<strong>Tintas</strong>
      </div>

      <div class="card-block">
        <div class="row">
          <div class="col-sm-4">
            <%= render 'shared/search_form' %>
          </div>

          <% if logged_in? %>
            <div class="col-sm-8 text-right">
              <%= link_to 'Nova Tinta',
                          new_estoque_tinta_path,
                          remote: true,
                          class: 'btn btn-success' %>

              <%= link_to 'Alterar Preços',
                          prices_estoque_tintas_path,
                          remote: true,
                          class: 'btn btn-info' %>
            </div>
          <% end %>
        </div>

        <div id="tin-tableview">
          <%= render 'table' %>
        </div>
      </div>
    </div>
  </div>
</div>
