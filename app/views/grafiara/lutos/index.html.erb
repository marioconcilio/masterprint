<% provide(:url, grafiara_lutos_path) %>
<% provide(:search_field, :referencia_cont) %>
<% provide(:placeholder, 'Referência') %>

<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <%= fa_tag 'fa fa-align-justify' %>
        Grafiara<strong>Estampas Luto</strong>
      </div>

      <div class="card-block">
        <div class="row">
          <div class="col-sm-4">
            <%= render 'shared/search_form' %>
          </div>

          <% if logged_in? %>
            <div class="col-sm-8 text-right">
              <%= link_to 'Nova Estampa',
                          new_grafiara_luto_path,
                          remote: true,
                          class: 'btn btn-success' %>
            </div>
          <% end %>
        </div>

        <div id="grafiara-lutos-tableview">
          <%= render 'table' %>
        </div>
      </div>
    </div>
  </div>
</div>
